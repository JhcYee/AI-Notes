# Database connection (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@your-neon-host.neon.tech/your_database?sslmode=require

# Optional: Individual PostgreSQL variables (if not using DATABASE_URL)
PGHOST=your-neon-host.neon.tech
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=your_database

# Session secret (generate a random string)
SESSION_SECRET=your-random-secret-string-here

# For local development without Replit AI Integrations, add your own OpenAI key:
# OPENAI_API_KEY=sk-your-openai-api-key
